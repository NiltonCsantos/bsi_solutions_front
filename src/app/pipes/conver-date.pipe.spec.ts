import { ConverDatePipe } from './conver-date.pipe';

describe('ConverDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverDatePipe();
    expect(pipe).toBeTruthy();
  });
});
